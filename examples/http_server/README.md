# HTTP server example

This makes your ESP32 serve a Hello World website in your LAN.

## How to use

1. Configure your WiFi SSID and password using `ESP-IDF: SDK Configuration Editor (Menuconfig)` command from the command palette.
2. Build and flash.
